{
  "name": "figma-mcp",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:20",

  "features": {
    "ghcr.io/devcontainers/features/node:1": { "version": "20" }
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "github.copilot",
        "github.copilot-chat"
      ],
      "settings": {
        "chat.mcp.enabled": true
      }
    }
  },

  "postCreateCommand": "corepack enable && corepack prepare pnpm@9 --activate && pnpm install && pnpm --filter figma-mcp exec playwright install chromium --with-deps && pnpm -r build && echo 'Setup complete. Run pnpm login-figma to authenticate, then open Copilot Chat (Agent) and paste a Figma URL.'",

  "forwardPorts": [],

  "remoteUser": "node"
}
